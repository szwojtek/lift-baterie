---
import { SITE } from '../config/site';
import { href } from '../utils/url';

interface Props {
  title?: string;
  subtitle?: string;
}

const {
  title = SITE.tagline,
  subtitle = 'Bezpłatny audyt baterii — pomiary, diagnoza i konkretna rekomendacja. Bez zobowiązań.',
} = Astro.props;
---

<section class="bg-ink-900">
  <div class="mx-auto max-w-6xl px-4 py-14 text-center">
    <h2 class="font-display text-2xl font-bold text-white sm:text-3xl">{title}</h2>
    <p class="mx-auto mt-3 max-w-2xl text-gray-300">{subtitle}</p>
    <div class="mt-7 flex flex-col items-center justify-center gap-3 sm:flex-row">
      <a
        href={href('/bezplatny-audyt')}
        class="w-full rounded-lg bg-brand-600 px-6 py-3 font-semibold text-white transition hover:bg-brand-500 sm:w-auto"
      >
        Zamów bezpłatny audyt
      </a>
      <a
        href={SITE.phoneHref}
        class="w-full rounded-lg border border-gray-500 px-6 py-3 font-semibold text-white transition hover:border-white sm:w-auto"
      >
        ☎ {SITE.phoneDisplay}
      </a>
    </div>
  </div>
</section>
